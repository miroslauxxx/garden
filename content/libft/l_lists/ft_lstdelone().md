```c
void ft_lstdelone(t_list *lst, void (*del)(void *));
```
DESCRIPTION:	
`Takes a node as parameter and frees its content using the function ’del’. Free the node itself but does NOT free the next node.`

RETURN VALUE:
`None`

#lists #delElement #free #libft

```NOTES 

``` 

```EDGE_CASES

```
SRC: 

MAN: