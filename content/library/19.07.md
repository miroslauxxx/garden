EOF is usually -1 and always is a value that an unsigned char can't hold

'C' or 'c' or '0' or any other character in C can be named as symbolic constant

A function provides a convenient way to encapsulate some computation, which can then be used without worrying about its implementation. With properly designed functions, it is possible to ignore how a job is done; knowing what is done is sufficient. When prototyping function names of parameters are optional, but types are required. Argument is the actual value of this variable that gets passed to function. Parameter is variable in the declaration of the function. The terms formal argument and actual argument are sometimes used for the same distinction. With properly designed functions, it is possible to ignore how a job is done; knowing what is done is sufficient.

An expression in C is a combination of operands and operators – it computes a single value stored in a variable. The operator denotes the action or operation to be performed. The operands are the items to which we apply the operation.

 https://www.geeksforgeeks.org/compiler-design/difference-between-definition-and-declaration/